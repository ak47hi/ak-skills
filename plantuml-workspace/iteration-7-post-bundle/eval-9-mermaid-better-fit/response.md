For a diagram that renders inline in a GitHub README, Mermaid is the right tool — GitHub's markdown engine renders Mermaid natively from a fenced ` ```mermaid ` block, while PlantUML needs an external renderer and either a committed image or a proxy URL.

Want me to draft a Mermaid flowchart with three or four steps, or do you already have a PlantUML renderer wired up and want a `.puml` file you can pipe through it?
