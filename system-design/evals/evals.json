{
  "skill_name": "system-design",
  "evals": [
    {
      "id": 0,
      "name": "notification-service-launch-spike",
      "prompt": "Design a notification service for 5M registered users. We send push, email, and SMS. The main load shape is launch announcements: a single dispatch fans out to all 5M users within ~5 minutes. Outside those events the system is mostly idle. The team is 3 backend engineers; we run on AWS and already have Postgres and Redis. Push goes via APNs/FCM, email via SendGrid, SMS via Twilio. We need a delivery audit log retained 90 days. p99 send-time within the 5-minute window is acceptable.",
      "files": [],
      "expected_output": "Phase 1 should pass (the prompt supplies enough constraints) or do a minimal propose-and-go for any small gaps. Phase 2 must produce a capacity estimate — the burst rate (~33k dispatches/sec for 5 min), the bandwidth split (small for push, large for email body), and the steady-state vs burst contrast. The estimate must drive the design: burst absorption is the binding problem, not raw scale. Phase 3 picks Postgres for audit (single instance fits easily) and SQS or similar queue (NOT Kafka — overkill at this rate). Phase 4 architecture: queue-and-drain with a fixed worker pool sized to the downstream providers' acceptable rate (APNs/FCM/SendGrid/Twilio limits). Phase 5 identifies the binding bottleneck as downstream provider rate limits + the broker, not the application; the scaling step is worker concurrency tuning, not horizontal app scaling. Phase 6 is the critical phase: per-channel timeouts, retry with backoff+jitter (idempotent — send retries must use idempotency keys so duplicate sends don't happen), dead-letter queue for permanent failures, degradation strategy (e.g., if email is down, the launch still goes via push). Phase 7 ADRs for: (a) queue choice (SQS vs Kafka vs Postgres-as-queue) with the boring-tech default; (b) idempotency key strategy; (c) DLQ retention/replay policy. Must explicitly REJECT or downscope: Kafka for 33k/sec sustained-burst-then-idle (mismatched workload), sharded queue, multi-region active-active. Should also note the email-body-as-bandwidth issue and suggest using SendGrid templates with personalization rather than sending full HTML.",
      "assertions": []
    },
    {
      "id": 1,
      "name": "postgres-95pct-reads-at-peak",
      "prompt": "Our Postgres database is falling over at peak. The workload is 95% reads, 5% writes — it's a product catalog for a retail site. We're seeing 60% CPU and connection saturation during peak hours. People on the team are talking about moving to microservices or sharding. We're on a single db.r6g.4xlarge in RDS. Read p99 is 800ms during peak (target: 200ms). About 50k QPS at peak.",
      "files": [],
      "expected_output": "Phase 1: the prompt supplies enough — proceed. The skill must DIAGNOSE BEFORE PRESCRIBING. Phase 2 estimate: 50k QPS at 95:5 read:write = ~47.5k reads/sec and ~2.5k writes/sec. Single-machine baseline says a properly-warmed Postgres absorbs 50k+ read QPS at the cached-working-set level; the symptom is wrong tuning or wrong access patterns, not 'need to shard.' Phase 3 (data): probable issues — working set exceeding shared_buffers; missing indexes; sequential scans on large tables; connection pool not in front (PgBouncer absent). Phase 5 scaling order: vertical (already at 4xlarge — try 8xlarge or 16xlarge), then read replicas (the workload is 95% read — this is the textbook case), then caching (Redis in front of the hot catalog reads). Sharding is several steps away and SHOULD BE EXPLICITLY DEFERRED. Microservices REJECTED — the database is the bottleneck, splitting the application into more services that hit the same database doesn't help; if anything it adds connection pressure. Phase 6: connection-pool overload is a failure mode now visible — PgBouncer is part of the fix. Phase 7 ADR for the chosen scaling path (replicas + cache before sharding). Output should be SHARP about diagnosis: ask for / state assumptions about pg_stat_statements, slow query log, working set vs RAM, connection pool config. Must NOT propose Kafka, NewSQL, Cassandra, or a microservices migration.",
      "assertions": []
    },
    {
      "id": 2,
      "name": "kafka-vs-postgres-as-queue-few-hundred-jph",
      "prompt": "We need a job queue for background processing — invoice PDF generation, webhook deliveries, email digests. Volume is ~300 jobs/hour, with bursts to ~1k/hour. The team has been debating Kafka vs using Postgres with SELECT FOR UPDATE SKIP LOCKED. Which should we use?",
      "files": [],
      "expected_output": "NARROW QUESTION. The skill MUST NOT emit the full 8-section template. It should give a short, scoped answer: name the binding signal (volume of ~300/hour with bursts to ~1k/hour = ~0.3 jobs/sec average, ~0.3 jobs/sec peak — laughably tiny for either system, which means operational cost dominates the decision). Recommend Postgres-as-queue (SKIP LOCKED) with explicit reasoning: complexity must be earned; Kafka is a real cluster to operate and 300 jobs/hour does not earn it; team probably already runs Postgres. State the SIGNAL THAT FLIPS THE ANSWER: when you'd reconsider (sustained >5–10k jobs/hour, multiple independent consumers needing the same stream, replay/backfill becomes a requirement, ordering across partitions matters). Boring-tech default applied. May briefly note SQS as a third option (managed, also fine, cheaper than running Kafka) but should NOT pad the answer with unrelated sections (no capacity estimate template, no failure-mode walk, no full ADR — at most a one-paragraph decision note). MATCH THE USER'S DEPTH.",
      "assertions": []
    },
    {
      "id": 3,
      "name": "rideshare-trip-history-plus-live-locations",
      "prompt": "Design the data layer for a ride-share app. Two main concerns: (1) trip history with payments — every completed trip records who, what route, what was charged, what payment method; financial accuracy is hard-required. (2) live driver locations — updated by every active driver every ~5 seconds, queried by the dispatch algorithm and the rider app for nearby-driver visualization. ~50k active drivers in a city at peak. Riders see driver pins on a map updated every few seconds.",
      "files": [],
      "expected_output": "Phase 1 acceptable — enough context. Critical move: CONSISTENCY IS CHOSEN PER OPERATION. Trip history + payments: linearizable / transactional, write-once-read-many; PostgreSQL the obvious pick. Live driver location: 50k drivers × write every 5s = ~10k writes/sec, mostly overwriting the same row; eventually-consistent is fine (a 5-second-old pin is acceptable); the read pattern is geospatial (find drivers within radius). Phase 3 must JUSTIFY POLYGLOT explicitly — these are genuinely different access patterns. Likely picks: Postgres + PostGIS (relational-first default; or Redis GEO commands for hot path), OR a dedicated geo store. The skill should COUNT CONSISTENCY BOUNDARIES (2 stores = 1 boundary; trip-record-on-trip-completion is the cross-boundary write — recorded in trips DB, last-location possibly updated in geo store separately, but they don't need to be transactionally consistent because the live location is ephemeral). Phase 4 architecture: write paths for the two systems are separate — no cross-store transaction needed because the operations are independent (you don't end a trip atomically with a location ping). Phase 6: payments has different failure semantics (no degradation — fail closed) vs locations (degrade gracefully — show last-known + a 'last updated N seconds ago' indicator). Phase 7 ADRs: (a) per-operation consistency split; (b) geo store choice with PostGIS-vs-Redis-GEO tradeoff; (c) explicit acknowledgment that this is justified polyglot, not accidental.",
      "assertions": []
    },
    {
      "id": 4,
      "name": "resilient-checkout-inventory-payments-fraud",
      "prompt": "Design a resilient checkout flow. The checkout calls three downstream systems synchronously: (1) inventory service to verify and reserve stock, (2) payments processor to authorize the charge, (3) fraud service to score the transaction. All three are external dependencies we don't own. We're seeing intermittent failures during peak and want to harden the design. Target: 99.9% checkout success rate when downstream systems are healthy; meaningful degradation when one is unhealthy.",
      "files": [],
      "expected_output": "Phase 1: enough context. The scope is Phase 6-heavy (failure modes). May abbreviate Phase 2/3 — the question is fundamentally a resilience question, not a sizing question. Phase 4 architecture: name the three sync calls explicitly and discuss WHICH are on the critical path (payments is — without it the charge doesn't happen; fraud-score is borderline — could be async pre-auth fraud + sync auth + post-auth review; inventory reserve is sync but the reservation can be a short TTL). Phase 6 must walk PER DEPENDENCY: (a) inventory — timeout (tight, e.g., 500ms), retry with backoff + jitter on idempotent reserve (idempotency key per cart), circuit breaker, fallback (cached stock with a 'stock may have changed' warning OR fail fast). (b) payments — timeout, IDEMPOTENCY KEY mandatory (duplicate charge is a real bug), retry only on network errors not on declines, circuit breaker, NO graceful fallback (don't 'just approve' if the processor is down — fail the checkout with retry guidance). (c) fraud — timeout, fallback to a CACHED OR DEFAULT SCORE so fraud being down doesn't block legitimate checkouts (this is the per-operation degradation decision — fraud failure shouldn't block 99% of legitimate customers). All three: bulkheads (separate connection pools per dependency). Phase 6 must include MINIMUM VIABLE FUNCTION for checkout: checkout requires payments and (with cached-fallback) fraud; inventory can fall back to a soft-reserve. Phase 7 ADRs: (a) per-dependency timeout/retry/circuit-breaker policy table; (b) fraud-degradation policy with cached-default fallback; (c) idempotency key scheme. Must mention the line 'untested failover is not failover' or its substance — runbooks for each circuit-breaker-open scenario.",
      "assertions": []
    }
  ]
}
